package View;

import javax.swing.*;

/**
 * @author 15252
 */
public class CombinedMainFrame extends JFrame {

   /* private JTabbedPane tabbedPane;

    public CombinedMainFrame() {
        setSize(800, 600);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setLocationRelativeTo(null);

        tabbedPane = new JTabbedPane();

        // 添加你的MainFrame到一个选项卡
        MainFrame TomatoTodo = new MainFrame();
        tabbedPane.addTab("番茄钟", TomatoTodo);

        // 添加另一个人的待办页面到另一个选项卡
        // 替换这里的 YourTodoFrameClass 为另一个人的待办页面类
        YourTodoFrameClass otherPersonTodoFrame = new YourTodoFrameClass();
        tabbedPane.addTab("其他人的待办", otherPersonTodoFrame);

        // 添加选项卡到主面板
        add(tabbedPane);
    }

    public static void main(String[] args) {
        SwingUtilities.invokeLater(() -> {
            CombinedMainFrame frame = new CombinedMainFrame();
            frame.setVisible(true);
        });
    }*/
}
